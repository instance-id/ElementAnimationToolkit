namespace instance.id.EATK.Extensions
{
    /// <summary>
    /// The type of container to use as a parent element when generating editor elements
    /// </summary>
    public enum ContainerType
    {
        AnimatedFoldout,
        Foldout,
        Box
    }
}
